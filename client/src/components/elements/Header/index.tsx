import { Button } from "@mantine/core";
import "./index.scss";

const Header = () => {
  return (
    <div className="header">
      <Button>LOG OUT</Button>
    </div>
  );
};

export default Header;
